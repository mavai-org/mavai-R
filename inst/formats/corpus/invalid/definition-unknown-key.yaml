# REFUSED: an unknown key on a service definition, named (II.5).
format: mavai-services/1
services:
  greeter:
    type: language-model
    retries: 3
    configuration:
      system-prompt: "You are a polite greeter."
