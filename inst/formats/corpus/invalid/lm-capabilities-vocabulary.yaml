# REFUSED: the capability allowance is a closed set (II.3; amendment
# 2026-07-25).
format: mavai-services/1
services:
  greeter:
    type: language-model
    configuration:
      system-prompt: "You are a polite greeter."
      capabilities: [telepathy]
