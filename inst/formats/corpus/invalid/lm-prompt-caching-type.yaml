# REFUSED: prompt-caching is a boolean (II.3, client factors).
format: mavai-services/1
services:
  greeter:
    type: language-model
    configuration:
      system-prompt: "You are a polite greeter."
      prompt-caching: "always"
