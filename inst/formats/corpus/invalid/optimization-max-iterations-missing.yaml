# REFUSED: max-iterations: is required — the cap always bounds the
# spend (Part I, optimizations).
format: mavai-services/1
services:
  basket-builder:
    type: language-model
    configuration:
      system-prompt: "You build baskets."
    optimizations:
      - stepper: linear-sweep
