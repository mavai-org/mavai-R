# REFUSED: only a string or value-comparison form may carry path: (Part I,
# subject rule) — satisfies: receives its subject whole.
format: mavai-contract/1
contract: c
service: s
transforms:
  basket: json
criteria:
  - threshold: 0.9
    postconditions:
      - in: basket
        path: "$.items"
        satisfies: looks-right
inputs: ["Alice"]
