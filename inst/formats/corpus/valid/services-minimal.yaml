# The specification's own minimal service definition (Part I): the
# code-free language-model service — a system prompt is all it takes.
format: mavai-services/1
services:
  greeter:
    type: language-model
    configuration:
      system-prompt: "You are a polite greeter."
