# Worked example: a criterion with no trials. Every sample failed to deliver,
# so the run executed nothing this criterion could count. trials is 0, the
# consumer must be able to tell "no evidence" from "field absent".
#
# observedPassRate is ABSENT, not 0.0. A rate is not stateable at zero
# trials, and asserting one from the same non-evidence that makes the bound
# null is exactly the confusion the null exists to prevent — so the rate is
# value-or-absent here, as the latency percentiles already are. The
# terminationReason records why the run stopped.
schemaVersion: "mavai-baseline-1"
serviceContractId: "consult-advice"
serviceName: "advice-model"
generatedAt: "2026-08-02T12:00:00Z"
confidenceLevel: 0.95
inputsIdentity: "3fd0a91c4e77"
covariateProfile:
  model: "advice-v2"
execution:
  samplesPlanned: 50
  samplesExecuted: 0
  terminationReason: "ABORTED_SERVICE_UNREACHABLE"
criteria:
  well-formed:
    mode: "inferential"
    procedure: "REGRESSION"
    trials: 0
    successes: 0
    wilsonLowerBound: null
contentFingerprint: "sha256:1111111111111111111111111111111111111111111111111111111111111111"
